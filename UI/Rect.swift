extension Msr.UI {
    enum FrameEdge {
        case Top
        case Bottom
        case Left
        case Right
    }
    enum FrameSizingDirection {
        case Horizontal
        case Vertical
    }
}
